// Server client creation is framework-dependent (needs next/headers).
// Use createServerClient from @supabase/ssr directly in your app code,
// or import the helper from @flex/web's lib/supabase-server.ts.
export { createServerClient } from "@supabase/ssr";
